package by.zinkov.victor.dao.impl;

import by.zinkov.victor.dao.GenericDao;
import by.zinkov.victor.dao.exception.DaoException;
import by.zinkov.victor.dao.factory.JdbcDaoFactory;
import by.zinkov.victor.domain.CargoType;
import by.zinkov.victor.domain.RegistrationKey;
import org.junit.Assert;
import org.junit.Test;


public class RegistrationKeyDaoTest {
    @Test
    public void insertRegistrationKeyDa() throws DaoException {
//        GenericDao<RegistrationKey,Integer> registrationKeyDao = JdbcDaoFactory.getInstance().getDao(RegistrationKey.class);
//        RegistrationKey key = new RegistrationKey();
//        key.setId(100);
//        key.setKey("random");
//        registrationKeyDao.persist(key);
//
//        RegistrationKey insertedKey = registrationKeyDao.getByPK(key.getId());
//        Assert.assertEquals(key,insertedKey);
    }

}